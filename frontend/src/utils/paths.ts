const PATHS = {
    HOME: "/",
    LOGIN: "/login",
    REGISTER: "/register",
    PROFILE: "/profile",
    CREATE_PROFILE: "/create-profile",
    MANAGE_PROFILE: "/manage-profile",
    SERVICES: "/services",
    BOOKINGS: "/bookings",
} as const;

export default PATHS;
